import type { Client, IGuild } from "../../index.js";
import { Method } from "./Method.js";
/**
 * @category Decorator
 */
export declare class DReaction extends Method {
    private _emoji;
    private _description;
    private _directMessage;
    private _guilds;
    private _botIds;
    private _aliases;
    private _remove;
    private _partial;
    get aliases(): string[];
    set aliases(value: string[]);
    get botIds(): string[];
    set botIds(value: string[]);
    get guilds(): IGuild[];
    set guilds(value: IGuild[]);
    get directMessage(): boolean;
    set directMessage(value: boolean);
    get emoji(): string;
    set emoji(value: string);
    get description(): string;
    set description(value: string);
    get remove(): boolean;
    set remove(value: boolean);
    get partial(): boolean;
    set partial(value: boolean);
    protected constructor(emoji: string, aliases?: string[], botIds?: string[], description?: string, directMessage?: boolean, guilds?: IGuild[], remove?: boolean, partial?: boolean);
    static create(emoji: string, aliases?: string[], botIds?: string[], description?: string, directMessage?: boolean, guilds?: IGuild[], remove?: boolean, partial?: boolean): DReaction;
    isBotAllowed(botId: string): boolean;
    getGuilds(client: Client): Promise<string[]>;
    isGuildAllowed(client: Client, guildId: string | null): Promise<boolean>;
    parseParams(): never[];
}
